version: '3.7'

services:
  nginx_dev:
    image: nginx:latest
    container_name: nginx_proxy
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - app
    networks:
      - network

  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: app
    container_name: app
    environment:
      - MONGO_URI=mongodb+srv://test:aPzuesWDWnkoCyR8@cluster0.gd6oq.mongodb.net/inwbot?retryWrites=true&w=majority
      - PORT=3000
    restart: always
    expose:
      - '3000'
    networks:
      - network

networks:
  network:
    driver: bridge
